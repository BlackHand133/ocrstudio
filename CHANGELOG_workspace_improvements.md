# Workspace Management System - Improvements & New Features

## สรุปการปรับปรุง (Summary)

เพิ่มฟีเจอร์การจัดการ workspace และ version ที่สมบูรณ์ยิ่งขึ้น ได้แก่:
1. **ลบ version** - สามารถลบ version ที่ไม่ต้องการได้ (พร้อมการป้องกันการลบ version สุดท้ายและ version ปัจจุบัน)
2. **ลบ workspace** - สามารถลบ workspace ทั้งหมดได้
3. **แก้ไขชื่อ workspace** - สามารถเปลี่ยนชื่อ workspace ได้โดยไม่กระทบ workspace_id และข้อมูล

---

## การเปลี่ยนแปลงในแต่ละไฟล์

### 1. `modules/workspace_manager.py`

#### ฟังก์ชันใหม่:

**`delete_version(workspace_id: str, version: str) -> Tuple[bool, str]`**
- ลบ version ที่ระบุออกจาก workspace
- **การป้องกัน:**
  - ไม่สามารถลบ version สุดท้าย (ต้องมีอย่างน้อย 1 version)
  - ไม่สามารถลบ version ที่กำลังใช้งาน (current version)
- ลบไฟล์ `v{N}.json` และอัพเดต `workspace.json`
- Returns: `(success: bool, message: str)`

**`get_version_list(workspace_id: str) -> List[Dict]`**
- ดึงรายการ version ทั้งหมดพร้อมข้อมูลละเอียด
- Returns: List of dict containing:
  - `version`: version name
  - `description`: คำอธิบาย
  - `created_at`: เวลาสร้าง
  - `modified_at`: เวลาแก้ไขล่าสุด
  - `is_current`: เป็น current version หรือไม่
  - `metadata`: สถิติ (total_images, annotated_images, total_annotations)

**`rename_workspace(workspace_id: str, new_name: str) -> Tuple[bool, str]`**
- เปลี่ยนชื่อ workspace (เปลี่ยนเฉพาะชื่อที่แสดง ไม่เปลี่ยน workspace_id)
- อัพเดตชื่อใน `workspace.json` และ `recent_workspaces.json`
- **การตรวจสอบ:** ชื่อใหม่ไม่เป็นค่าว่าง
- Returns: `(success: bool, message: str)`

**`_update_recent_workspace_name(workspace_id: str, new_name: str)`**
- ฟังก์ชันช่วยสำหรับอัพเดตชื่อใน recent_workspaces.json

---

### 2. `modules/gui/window_handler/workspace_handler.py`

#### ฟังก์ชันใหม่:

**`delete_version(version: str) -> tuple`**
- Wrapper สำหรับเรียก `workspace_manager.delete_version()`
- ตรวจสอบว่ามี workspace โหลดอยู่หรือไม่

**`get_version_list() -> List[Dict]`**
- Wrapper สำหรับเรียก `workspace_manager.get_version_list()`

**`rename_workspace(new_name: str) -> tuple`**
- Wrapper สำหรับเรียก `workspace_manager.rename_workspace()`

**`delete_workspace(workspace_id: str = None) -> bool`**
- Wrapper สำหรับเรียก `workspace_manager.delete_workspace()`
- ถ้าลบ workspace ปัจจุบัน จะ clear state ทั้งหมด

---

### 3. `modules/gui/workspace_selector_dialog.py`

#### การปรับปรุง WorkspaceSelectorDialog:

**ปุ่มใหม่:**
- **Rename** - เปลี่ยนชื่อ workspace ที่เลือก
- **Delete** - ลบ workspace ที่เลือก (พร้อมข้อความยืนยัน)

**ฟังก์ชันใหม่:**

**`rename_workspace()`**
- แสดง Input Dialog สำหรับกรอกชื่อใหม่
- เรียก `workspace_manager.rename_workspace()`
- รีเฟรชรายการหลังเปลี่ยนชื่อสำเร็จ

**`delete_workspace()`**
- แสดงข้อความยืนยันพร้อมคำเตือน
- เรียก `workspace_manager.delete_workspace()`
- รีเฟรชรายการหลังลบสำเร็จ

**การปรับปรุง `_on_selection_changed()`:**
- เปิด/ปิดปุ่ม Rename และ Delete ตามการเลือก workspace

---

### 4. `modules/gui/version_manager_dialog.py` (ไฟล์ใหม่)

Dialog สำหรับจัดการ version ทั้งหมดในรูปแบบตาราง

**คุณสมบัติ:**
- **แสดงรายการ version** ในรูปแบบตาราง พร้อมข้อมูล:
  - Version name
  - Description
  - Created date
  - Modified date
  - จำนวน annotations
  - Status (current version ถูกไฮไลท์)
- **แสดงรายละเอียด** version ที่เลือกใน Info Panel
- **Switch to Version** - สลับไป version ที่เลือก
- **Delete Version** - ลบ version ที่เลือก
- **Refresh** - รีเฟรชรายการ

**การป้องกัน:**
- ปุ่ม Switch และ Delete จะปิดการใช้งานสำหรับ current version
- ข้อความยืนยันก่อนลบ version

---

### 5. `modules/gui/ui_components.py`

#### เมนูใหม่ใน Workspace Menu:

**"Manage Versions"**
- เปิด VersionManagerDialog สำหรับจัดการ version ทั้งหมด
- แสดงรายการ version, สลับ version, ลบ version

**"Rename Workspace"**
- เปลี่ยนชื่อ workspace ปัจจุบัน

---

### 6. `modules/gui/main_window.py`

#### ฟังก์ชันใหม่:

**`manage_versions()`**
- เปิด VersionManagerDialog
- รีเฟรช UI หลังจากมีการเปลี่ยนแปลง version

**`rename_current_workspace()`**
- แสดง Input Dialog สำหรับกรอกชื่อใหม่
- เรียก `workspace_handler.rename_workspace()`
- อัพเดต UI หลังเปลี่ยนชื่อสำเร็จ

---

## การใช้งาน (How to Use)

### การลบ Version:

**วิธีที่ 1: ผ่านเมนู**
1. คลิก **Workspace → Manage Versions**
2. เลือก version ที่ต้องการลบ
3. คลิก **Delete Version**
4. ยืนยันการลบ

**ข้อจำกัด:**
- ไม่สามารถลบ version สุดท้ายได้
- ไม่สามารถลบ version ที่กำลังใช้งานได้ (ต้อง switch ไป version อื่นก่อน)

### การลบ Workspace:

1. ปิด workspace ปัจจุบัน หรือสลับไป workspace อื่น
2. คลิก **Workspace → Switch Workspace**
3. เลือก workspace ที่ต้องการลบ
4. คลิก **Delete**
5. ยืนยันการลบ (จะลบข้อมูลทั้งหมดถาวร)

### การแก้ไขชื่อ Workspace:

**วิธีที่ 1: จากหน้าต่างหลัก**
1. เปิด workspace ที่ต้องการเปลี่ยนชื่อ
2. คลิก **Workspace → Rename Workspace**
3. กรอกชื่อใหม่
4. กด OK

**วิธีที่ 2: จาก Workspace Selector**
1. คลิก **Workspace → Switch Workspace**
2. เลือก workspace ที่ต้องการเปลี่ยนชื่อ
3. คลิก **Rename**
4. กรอกชื่อใหม่
5. กด OK

**หมายเหตุ:** การเปลี่ยนชื่อ workspace จะเปลี่ยนเฉพาะชื่อที่แสดง ไม่กระทบ workspace_id หรือโครงสร้างไฟล์

---

## การป้องกันข้อมูล (Data Protection)

### ✅ การป้องกันที่มีอยู่:

1. **ป้องกันการลบ version สุดท้าย**
   - ต้องมีอย่างน้อย 1 version ใน workspace

2. **ป้องกันการลบ current version**
   - ต้อง switch ไป version อื่นก่อนจึงจะลบได้

3. **ข้อความยืนยันก่อนลบ**
   - แสดงข้อความเตือนและยืนยันทุกครั้งก่อนลบข้อมูล

4. **ไม่กระทบข้อมูลจริง**
   - การเปลี่ยนชื่อ workspace ไม่เปลี่ยน workspace_id
   - ไม่กระทบโครงสร้างไฟล์หรือข้อมูล annotations

5. **Auto-save ก่อนสลับ version**
   - บันทึก version ปัจจุบันอัตโนมัติก่อนสลับหรือลบ

---

## โครงสร้างไฟล์ไม่เปลี่ยนแปลง

โครงสร้างไฟล์ยังคงเหมือนเดิม:

```
workspaces/
├── {workspace_id}/
│   ├── workspace.json          # Workspace metadata
│   ├── v1.json                 # Version 1 data
│   ├── v2.json                 # Version 2 data (ถ้ามี)
│   ├── v{N}.json               # Version N data
│   └── exports.json            # Export history
```

การลบ version จะลบเฉพาะไฟล์ `v{N}.json` และอัพเดต `workspace.json`

---

## ทดสอบ (Testing Checklist)

- [ ] สร้าง workspace ใหม่
- [ ] สร้าง version ใหม่ (v2, v3)
- [ ] ลองลบ version ที่ไม่ใช่ current version
- [ ] ตรวจสอบว่าไม่สามารถลบ current version
- [ ] ตรวจสอบว่าไม่สามารถลบ version สุดท้าย
- [ ] สลับระหว่าง version และตรวจสอบข้อมูล
- [ ] เปลี่ยนชื่อ workspace และตรวจสอบว่าข้อมูลยังอยู่
- [ ] ลบ workspace และตรวจสอบว่าข้อมูลถูกลบหมด
- [ ] ตรวจสอบ recent_workspaces.json อัพเดตถูกต้อง

---

## ข้อควรระวัง (Warnings)

⚠️ **การลบเป็นการถาวร (Permanent Deletion)**
- การลบ version หรือ workspace จะลบข้อมูลทั้งหมดอย่างถาวร
- ไม่สามารถกู้คืนได้
- ควรสำรองข้อมูลก่อนลบ

⚠️ **ตรวจสอบก่อนลบ**
- ดูจำนวน annotations ก่อนตัดสินใจลบ
- ตรวจสอบว่าเป็น version ที่ถูกต้องหรือไม่

---

## Bug Fixes (เพิ่มเติม)

### ปัญหา: Version ที่ไม่มีอยู่จริงปรากฏในรายการ

**อาการ:**
- Version (เช่น v3) ปรากฏในรายการ Switch Version แต่ไฟล์ v3.json ไม่มีอยู่จริง
- เกิดจากการลบไฟล์โดยตรง หรือข้อผิดพลาดในการสร้าง version

**การแก้ไข:**

1. **Auto-Repair เมื่อโหลด Workspace** (`_validate_and_repair_workspace()`)
   - ตรวจสอบว่า version ใน available list มีไฟล์จริงหรือไม่
   - ลบ version ที่ไม่มีไฟล์ออกจาก list อัตโนมัติ
   - ตรวจสอบ current version ว่ายังมีอยู่หรือไม่
   - บันทึกการแก้ไขกลับเข้า workspace.json

2. **Manual Repair** (`repair_workspace()`)
   - ฟังก์ชันใหม่สำหรับซ่อมแซม workspace ด้วยตัวเอง
   - หา version files ที่มีจริงในโฟลเดอร์
   - เปรียบเทียบกับ available list และแก้ไข
   - แสดงรายละเอียดการแก้ไข

3. **UI: ปุ่ม Repair** ใน Workspace Selector Dialog
   - ปุ่ม "🔧 Repair" สำหรับซ่อมแซม workspace
   - แสดงข้อความยืนยันและรายละเอียดการซ่อมแซม

**ไฟล์ที่แก้ไข:**
- `modules/workspace_manager.py`
  - เพิ่ม `_validate_and_repair_workspace()` - Auto-repair
  - เพิ่ม `repair_workspace()` - Manual repair
  - แก้ไข `load_workspace()` - เรียก auto-repair ทุกครั้ง

- `modules/gui/workspace_selector_dialog.py`
  - เพิ่มปุ่ม "🔧 Repair"
  - เพิ่มฟังก์ชัน `repair_workspace()`

**ผลลัพธ์:**
- ✅ Version ที่ไม่มีไฟล์จริงจะถูกลบอัตโนมัติ
- ✅ Current version จะถูกแก้ไขหากไม่มีอยู่จริง
- ✅ ผู้ใช้สามารถซ่อมแซม workspace ด้วยตัวเองได้

---

## เวอร์ชัน

- **Version:** 2.0.2
- **Date:** 2025-11-06
- **Author:** Claude Code Assistant
