# Lightweight CI/test dependencies.
#
# Runs the web API round-trip tests (tests/test_api_server.py) and the pure
# unit tests WITHOUT the heavy paddle stack or PyQt5 — paddle is only imported
# lazily inside detector.detect(), which the tests stub out with a FakeDetector.
pytest>=7.4
pytest-cov>=4.1

# Web backend (mirrors requirements-server.txt minus uvicorn, which TestClient
# does not need).
fastapi>=0.110,<1.0
python-multipart>=0.0.9
pydantic>=2.5,<3.0
# fastapi.testclient.TestClient transport. Starlette 1.2+ looks for httpx2
# first and only falls back to httpx with a deprecation warning.
httpx2>=2.0
starlette>=1.2

# Core libraries used by the headless import chain
# (image_service thumbnails, export crop/mask utils, validation, augmentation).
# Upper bounds: see requirements.txt.
opencv-python-headless>=4.8.0,<6
Pillow>=10.0.0,<13
numpy>=1.24.0,<3.0.0
PyYAML>=6.0.0,<7
